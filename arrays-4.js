const array = ['C','C++','C#','JavaScript','Java','Python','Basic','Kotlin','Swift','TypeScript']
let i = 0; 

while (i < array.length) { 
  if (array[i].length > 3) {
    console.log(array[i]); // 
  }
  i++; 
}